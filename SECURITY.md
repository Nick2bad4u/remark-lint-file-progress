# Security

Report vulnerabilities through the repository's GitHub private vulnerability reporting interface. Do not include credentials or private source paths in public issues.

Progress output contains paths from remark/unified VFiles. Choose basename paths or hide filenames when logs should contain less location detail. Output is observational and never executes filename or option text.
