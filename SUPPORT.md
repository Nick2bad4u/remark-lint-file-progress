# Support

Open a GitHub issue with the Node, remark, unified, and plugin versions, plugin configuration, command, and minimal Markdown that reproduces the behavior. Remove credentials and private paths from logs before sharing them.

For summary questions, include whether the command uses the CLI, Node API, an editor, host-level skipping, or a watch process. Those choices determine which inputs reach the progress transformer and when the process exits.
